import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  tabBar: {
    backgroundColor: '#fafbfc',
    borderTopWidth: 0,
    shadowColor: '#e1e4e8',
    shadowOffset: {
      height: -1,
    },
    shadowOpacity: 1,
    shadowRadius: 1,
  },
});
